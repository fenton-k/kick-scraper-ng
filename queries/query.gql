query ($slug: String!) {
  project(slug: $slug) {
    id
    creator {
      name
      launchedProjects {
        totalCount
      }
    }
    state
    collaborators {
      edges {
        node {
          name
        }
      }
    }
  }
}
