query ($nextCursor: String) {
  projects(first: 10, after: $nextCursor, state: LIVE, sort: NEWEST) {
    edges {
      node {
        id
        name
        slug
        url
        percentFunded
        pledged {
          amount
          currency
        }
        description
        backersCount
        deadlineAt
        isLaunched
        isProjectOfTheDay
        isProjectWeLove

        creator {
          name
          url
          launchedProjects {
            totalCount
          }
        }

        collaborators {
          edges {
            node {
              name
            }
          }
        }
      }
      cursor
    }

    pageInfo {
      hasNextPage
      endCursor
    }
  }
}